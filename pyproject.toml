[project]
name = "anthropic-building-with-claude-api"
version = "0.1.0"
description = "Anthropic course for building with Claude api"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.117.0",
    "python-dotenv>=1.2.2",
]
